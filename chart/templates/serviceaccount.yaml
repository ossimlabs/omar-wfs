{{- if eq (include "omar-wfs.serviceAccount.enabled" $) "true" -}}
{{- $annotations := merge .Values.serviceAccount.annotations $.Values.global.serviceAccount.annotations -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "omar-wfs.serviceAccount.name" . }}
  labels:
    {{- include "omar-wfs.labels" . | nindent 4 }}
  {{- with $annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
